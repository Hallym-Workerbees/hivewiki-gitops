apiVersion: apps/v1
kind: Deployment
metadata:
  name: hivewiki-builder
  labels:
    app: hivewiki-builder
    app.kubernetes.io/name: hivewiki-builder
    app.kubernetes.io/part-of: hivewiki
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: hivewiki-builder
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: hivewiki-builder
      labels:
        app: hivewiki-builder
        app.kubernetes.io/name: hivewiki-builder
        app.kubernetes.io/part-of: hivewiki
    spec:
      automountServiceAccountToken: false
      restartPolicy: Always
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: Always
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: hivewiki-builder
          image: 647502392199.dkr.ecr.ap-northeast-2.amazonaws.com/hivewiki-builder:dev-837670c
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: hivewiki-builder-env
            - secretRef:
                name: hivewiki-builder-secrets
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 24
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
